name: Dummy
description: Examples for Migrate API
type: module
core: 8.x
version: 1.x

dependencies:
 - migrate
 - migrate_plus
 - migrate_tools
 - config_devel

config_devel:
  install:
    - migrate_plus.migration_group.dummy
    - migrate_plus.migration.dummy_users
    - migrate_plus.migration.dummy_article_files
    - migrate_plus.migration.dummy_articles